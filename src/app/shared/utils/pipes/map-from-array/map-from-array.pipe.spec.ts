import { MapFromArrayPipe } from './map-from-array.pipe';

describe('MapFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MapFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
