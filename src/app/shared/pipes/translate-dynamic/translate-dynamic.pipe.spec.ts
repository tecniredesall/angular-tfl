import { TranslateDynamicPipe } from './translate-dynamic.pipe';

describe('TranslateDynamicPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateDynamicPipe();
    expect(pipe).toBeTruthy();
  });
});
