import { SearchByPropertyPipe } from './search-by-property.pipe';

describe('SearchByPropertyPipe', () => {
    it('create an instance', () => {
        const pipe = new SearchByPropertyPipe();
        expect(pipe).toBeTruthy();
    });
});
